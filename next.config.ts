import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Vercel handles image optimization automatically
  // No need for unoptimized flag - images will be optimized by default
};

export default nextConfig;
